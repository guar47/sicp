;Applicative-order evaluation:
;  The function will trying to evaluate the argument '(p)' and will go into an infinite recursion.
;Normal-order evaluation:
;  The function will not trying to evaluate the arguments and will go to if condition who return 0.
